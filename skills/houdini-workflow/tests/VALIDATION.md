# Houdini 市场适配验证

## 来源与范围

来源：用户提供的 `Houdini-Design-Skills-20260910.zip`。整合其四个 Skill 为 `houdini-workflow` 一个入口，保留工程分析、PCG、VEX 与美术/节奏资料。来源包留存于用户下载目录，原始归档不改动。

本次未导入主仓库源码快照、离线连接器、旧桥接、历史知识库及私人路径，也未把原来源的实机通过声明移作本次测试结果。两个大目录 `PATTERNS.md`、`VEX_RECIPES.md` 作为按需参考保留，历史示例与统计需在当前任务重新核验。

## 定向验证

- Python 标准库测试：51 项通过。覆盖继承的连接准备/版本与实例选择/工作区身份拒绝/超时保留进程，以及新增的 HIP 损坏输入、路径越界、符号链接、已有目录保护和跨网络提取。
- 原包两项路径比较改为规范化路径比较，兼容 macOS `/var` 与 `/private/var` 指向同一位置；不改变启动判定。
- 视频分析脚本：用临时生成的 2 秒 320×180、24fps、带声音测试片，实际运行抽帧与分析，验证媒体信息、音频摘要和接触表生成。
- Python 脚本语法、两个入口的长度/命名及 Markdown 链接校验通过。
- 市场全量校验通过（115 个 Skill、6 个 Plugin）；索引只新增 Houdini 条目。
- 触发用例与工作流见 `trigger-cases.md`，属于预期和静态走查，未跑真实 Agent 触发测试。

## 未验证项

- 未在 Houdini 实机执行安装、冷启动、建模、模拟、保存工程或画布回传。
- 原始连接证据主要来自 Windows；不能据此宣称 Mac 可用。
- HIP 静态解析仅覆盖旧 ASCII cpio 容器，不承诺解析所有版本/加密格式/插件语义。新增用合成容器验证，尚未用真实用户 HIP 全链验证。
- 连接准备依赖客户端提供托管安装及正确的注册布局；市场 Skill 不会安装缺失的客户端接口或自动发布服务端静默安装白名单。
